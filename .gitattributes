# Only track files in dist/UmoAuthSdk directory with LFS (for CocoaPods)
# This ensures root-level files like LICENSE, Package.swift work with SPM

# Track entire XCFramework directories
dist/UmoAuthSdk/*.xcframework/** filter=lfs diff=lfs merge=lfs -text

# Explicitly exclude certain patterns if needed
*.podspec !filter !diff !merge text
Package.swift !filter !diff !merge text
.gitignore !filter !diff !merge text
